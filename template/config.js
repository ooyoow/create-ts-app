// This javascript is executed before any other script
// Use this file to define any variable that the client
// app would need (i.e. webapi endpoint etc..)

// the embedded node server will deploy a config.js that includes
// all the REACT_APP_* environment variables.

// Default Configuration
var REACT_APP_SERVICE_URI = "http://localhost:5000";
var REACT_APP_SERVICE_API_PATTERN = "api/v0";

